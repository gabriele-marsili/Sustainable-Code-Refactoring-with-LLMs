#include "binary_search_tree.h"

namespace binary_search_tree {
    template<typename T>
    T& binary_tree<T>::data() const {
        return &_data;
    }

    template<typename T>
    T& binary_tree<T>::left() const {

    }

    template<typename T>
    T& binary_tree<T>::right() const {

    }

    template<typename T>
    void binary_tree<T>::insert(T item) {

    }
}  // namespace binary_search_tree
